# =============================================================================
# FACTOSYS STORE WEB — único archivo de entorno
# Local:  cp .env.example .env
# Producción (Coolify): pega este .env en bulk y cambia NODE_ENV=production
# =============================================================================

NODE_ENV=development
HOST=0.0.0.0
PORT=3000
NITRO_HOST=0.0.0.0
NITRO_PORT=3000

NUXT_PUBLIC_API_BASE_URL=/api
NUXT_API_PROXY_TARGET=https://127.0.0.1:3000
NUXT_PUBLIC_APP_NAME=Factosys Store
